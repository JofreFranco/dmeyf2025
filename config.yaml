# Configuración del experimento Experimento1
experiment:
  name: "DeltaLags2" # Indica la features probadas
  tag: "0.5_50_01-02_G" # "sampling_trials_from-to_feval" tag para indicar el tipo de experimento
  version: "0.0.1" # Version de código src
  debug: false
  sample_ratio: 0.5
  n_trials: 50
  n_init: 20 # Exploración de optuna.
  description: "Experimento Deltas y Lags de orden 2. Optimizando por ganancia, clase positiva durante training = (BAJA+1 y BAJA+2). Escalado de hiperparámetros."
  experiments_path: "./experiments"
  data_path: "./data"
  raw_data_path: "./data/competencia_01_crudo.csv"
  result_path: "./results"
  commit: True

hyperparameters:
  num_leaves:
    type: "int"
    min: 80
    max: 1000
  learning_rate:
    type: "float" 
    min: 0.005
    max: 0.1
  min_data_in_leaf:
    type: "int"
    min: 1
    max: 5000
  feature_fraction:
    type: "float"
    min: 0.01
    max: 1.0
  bagging_fraction:
    type: "float"
    min: 1.0
    max: 1.0
  num_boost_round:
    type: "int"
    min: 100
    max: 10000
  early_stopping_rounds:
    type: "int"
    min: 50
    max: 50
data:
  train_months: [202101, 202102]
  test_month: 202104
  eval_month: 202106
weights:
  continue: 1
  BAJA+1: 1
  BAJA+2: 1

